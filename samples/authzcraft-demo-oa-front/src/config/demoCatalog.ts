import type { BusinessPageConfig, Scenario } from '../types/domain'

export const theme = {
  token: {
    colorPrimary: '#185c4c',
    colorSuccess: '#0f7b61',
    colorWarning: '#b46622',
    colorError: '#b73737',
    borderRadius: 8,
    fontFamily: 'Aptos Display, Microsoft YaHei UI, DIN Alternate, sans-serif',
  },
}

export const scenarios: Scenario[] = [
  {
    key: 'meetings',
    title: '会议记录',
    resourceKey: 'WB_MEETING',
    endpoint: '/authzcraft-demo-oa/api/v1/meetings/search',
    countEndpoint: '/authzcraft-demo-oa/api/v1/meetings/count',
    columns: ['topic', 'host_user_id', 'host_dept_code', 'start_time', 'duration_minutes'],
    expectedAccessPathKeys: ['WB_MEETING_TO_PARTICIPANT'],
    accessPathText: '按参会人所在部门判定：会议有参会人属于管辖部门才可见',
    requiredPermission: 'demo-oa:meetings:read',
  },
  {
    key: 'meetingParticipants',
    title: '会议参会人',
    resourceKey: 'WB_MEETING_PARTICIPANT',
    endpoint: '/authzcraft-demo-oa/api/v1/meeting-participants/search',
    countEndpoint: '/authzcraft-demo-oa/api/v1/meeting-participants/count',
    columns: ['meeting_id', 'participant_user_id', 'participant_dept_code', 'attendance_minutes'],
    expectedAccessPathKeys: [],
    accessPathText: '按参会人所在部门判定：属于管辖部门的参会人才可见',
    requiredPermission: 'demo-oa:meeting-participants:read',
  },
  {
    key: 'leaveRequests',
    title: '请假单',
    resourceKey: 'WB_LEAVE_REQUEST',
    endpoint: '/authzcraft-demo-oa/api/v1/leave-requests/search',
    countEndpoint: '/authzcraft-demo-oa/api/v1/leave-requests/count',
    columns: ['applicant_user_id', 'applicant_name', 'applicant_dept_code', 'leave_type', 'start_date', 'end_date', 'leave_days', 'reason', 'status'],
    expectedAccessPathKeys: [],
    accessPathText: '按申请人所在部门判定：属于管辖部门的请假单才可见',
    requiredPermission: 'demo-oa:leave-requests:read',
    formFields: [
      { field: 'applicant_user_id', label: '申请人ID' },
      { field: 'applicant_name', label: '申请人姓名' },
      { field: 'applicant_dept_code', label: '部门编码' },
      { field: 'leave_type', label: '请假类型', type: 'select', options: [
        { label: '事假', value: 'PERSONAL' },
        { label: '病假', value: 'SICK' },
        { label: '年假', value: 'ANNUAL' },
        { label: '调休', value: 'COMPENSATORY' },
        { label: '婚假', value: 'MARRIAGE' },
      ] },
      { field: 'start_date', label: '开始日期' },
      { field: 'end_date', label: '结束日期' },
      { field: 'leave_days', label: '请假天数', type: 'number' },
      { field: 'reason', label: '事由' },
      { field: 'status', label: '状态', type: 'select', options: [
        { label: '草稿', value: 'DRAFT' },
        { label: '已提交', value: 'SUBMITTED' },
        { label: '已通过', value: 'APPROVED' },
      ] },
    ],
  },
  {
    key: 'expenseRequests',
    title: '费用申请单',
    resourceKey: 'WB_EXPENSE_REQUEST',
    endpoint: '/authzcraft-demo-oa/api/v1/expense-requests/search',
    countEndpoint: '/authzcraft-demo-oa/api/v1/expense-requests/count',
    columns: ['request_no', 'title', 'applicant_user_id', 'dept_code', 'amount', 'reason', 'status'],
    expectedAccessPathKeys: [],
    accessPathText: '按申请人所在部门判定：属于管辖部门的费用申请单才可见',
    requiredPermission: 'demo-oa:expense-requests:read',
    formFields: [
      { field: 'request_no', label: '申请编号' },
      { field: 'title', label: '标题' },
      { field: 'applicant_user_id', label: '申请人ID' },
      { field: 'dept_code', label: '部门编码' },
      { field: 'expense_type', label: '费用类型' },
      { field: 'amount', label: '金额', type: 'number' },
      { field: 'reason', label: '事由' },
      { field: 'status', label: '状态', type: 'select', options: [
        { label: '草稿', value: 'DRAFT' },
        { label: '已提交', value: 'SUBMITTED' },
        { label: '已通过', value: 'APPROVED' },
      ] },
    ],
  },
  {
    key: 'leaveApprovals',
    title: '请假审批',
    resourceKey: 'WB_LEAVE_REQUEST_APPROVAL',
    endpoint: '/authzcraft-demo-oa/api/v1/leave-approvals/search',
    countEndpoint: '/authzcraft-demo-oa/api/v1/leave-approvals/count',
    columns: ['leave_request_id', 'approver_user_id', 'approver_name', 'action', 'approved_at'],
    expectedAccessPathKeys: [],
    accessPathText: '按审批人判定：只能看到自己审批过的记录',
    requiredPermission: 'demo-oa:leave-approvals:read',
  },
]

export const businessPageConfigs: BusinessPageConfig[] = [
  {
    key: 'meeting-analytics',
    title: '会议信息',
    description: '围绕会议记录和参会明细做组织协同分析，展示会议覆盖、参会时长和访问路径证据。',
    permissionLabel: '会议数据处理',
    requiredPermissions: ['demo-oa:meetings:read', 'demo-oa:meeting-participants:read'],
    scenarioKeys: ['meetings', 'meetingParticipants'],
    dataScopeText: '会议记录由参会人部门访问路径裁剪，参会人明细按参会人部门裁剪。',
    masterDetail: { childScenarioKey: 'meetingParticipants', joinField: 'meeting_id' },
  },
  {
    key: 'expense-applications',
    title: '费用申请',
    description: '展示费用申请单处理队列、金额字段权限和部门数据范围，适合演示字段脱敏与行过滤协同。',
    permissionLabel: '费用数据处理',
    requiredPermissions: ['demo-oa:expense-requests:read'],
    scenarioKeys: ['expenseRequests'],
    dataScopeText: '费用申请单按申请人部门裁剪，金额字段由后端字段权限决定完整展示或脱敏。',
  },
  {
    key: 'leave-applications',
    title: '请假申请',
    description: '展示请假单与审批记录联动视图，突出功能权限、审批人视角和数据范围边界。',
    permissionLabel: '请假数据处理',
    requiredPermissions: ['demo-oa:leave-requests:read', 'demo-oa:leave-approvals:read'],
    scenarioKeys: ['leaveRequests', 'leaveApprovals'],
    dataScopeText: '请假单按申请人部门裁剪，审批记录按审批人或授权策略裁剪。',
    masterDetail: { childScenarioKey: 'leaveApprovals', joinField: 'id', childJoinField: 'leave_request_id' },
  },
]
