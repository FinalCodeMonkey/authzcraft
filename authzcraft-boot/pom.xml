<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.fcm</groupId>
        <artifactId>authzcraft-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../parent/pom.xml</relativePath>
    </parent>

    <artifactId>authzcraft-boot</artifactId>
    <packaging>jar</packaging>

    <name>authzcraft-boot</name>
    <description>AuthzCraft Spring Boot application.</description>

    <dependencies>
        <dependency>
            <groupId>com.fcm</groupId>
            <artifactId>authzcraft-pap</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fcm</groupId>
            <artifactId>authzcraft-pdp</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fcm</groupId>
            <artifactId>authzcraft-pip</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fcm</groupId>
            <artifactId>authzcraft-audit</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.fcm.authzcraft.AuthzCraftApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>